name: dog-vision
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.12  ## specify desired version
  - pip

  ## PyPI packages
  - pip:
    - tensorflow
    - tensorflow-metal  ## optional, but recommended
