name: dog-vision
channels:
  - defaults
dependencies:
  - python=3.12  ## specify desired version
  - pip

  # General tools
  - cytoolz
  - ipython
  - jupyterlab

  # Data analysis
  - numpy
  - matplotlib
  - pandas

  # Scikit-learn
  - scikit-learn

  ## PyPI packages
  - pip:
    - tensorflow
    - tensorflow-metal  ## optional, but recommended
    - tensorflow_hub  ## A repository of trained machine learning models
