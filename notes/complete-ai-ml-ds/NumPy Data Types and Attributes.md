We'll start to right some `numpy` code
- Navigate to the directory containing our notebooks
- Activate `condo` using the `complete-ai-ml-ds` environment

The anatomy of a `NumPy array`
- ![[Anatomy of an NumPy Array.png]]

Create a new `jupyter lab notebook

