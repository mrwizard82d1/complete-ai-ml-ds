What we're covering
1. An end-to-end Scikit-Learn workflow
2. Getting the data ready
3. Choose the right estimator/algorithm for our problems
4. Fit the model/algorithm and use it to make predictions on our data
5. Evaluating a model
6. Improve a model
7. Save and load a trained model
8. Putting it all together


We'll be covering **much, much, much** material
- So we've made an auxillary resource, [Scikit-learn Reference Notebook](https://github.com/mrdbourke/zero-to-mastery-ml/blob/master/section-2-data-science-and-ml-tools/scikit-learn-what-were-covering.ipynb)
- Learning something new **takes time** (and **effort**)

Our workflow
- Get data ready
- Pick a model (to suit your problem)
- Fit the model to the data and make a prediction
- Evaluate the model
- Improve through experimentation
- Save a reload your trained model

At the top,
- Standard library imports

We'll use 2 datasets for demonstrations
- `heart_disease` - a classification dataset (predicting whether or not someone has heart disease)
- `boston_df` - a regression dataset (predicting the median house prices in the city of Boston)

Getting the data ready
Pick a model or estimator (to suit your problem)
Fit the model to the data and make a prediction
Evaluate the model
- Highly recommend reading documentation
Improving through experimentation
- Hyperparameter tuning
Save and reload trained model

Putting it all together (in a **single** cell)
