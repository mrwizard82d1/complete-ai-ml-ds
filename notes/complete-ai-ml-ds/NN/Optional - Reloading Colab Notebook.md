What to do if I see a "Runtime disconnected" warning in Google Colab

Two steps:
- Reconnect
- Re-run notebook

Remember that re-running a notebook may take some time
- Might be able to lessen time (in Colab) by only running cells
	- Whose results are used by other cells
	- (Not just "output")
